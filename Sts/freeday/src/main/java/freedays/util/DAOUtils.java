package freedays.util;

import java.util.List;

import javax.persistence.TypedQuery;

/**
 * Utility class used to return single result from a query. If a query is null
 * then it throws an exception. Instead used a check and return null.
 * 
 * @author gparvu
 * 
 */
public class DAOUtils {
//	public static <T> T getSingleResult(TypedQuery<T> q) {
//		List<T> results = q.getResultList();
//		if (!results.isEmpty()) {
//			return results.get(0);
//		}
//		return null;
//	}
}

