// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package freedays.app;

import freedays.app.HRManagement;
import java.security.SecureRandom;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect HRManagementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: HRManagementDataOnDemand: @Component;
    
    private Random HRManagementDataOnDemand.rnd = new SecureRandom();
    
    public HRManagement HRManagementDataOnDemand.getNewTransientHRManagement(int index) {
        HRManagement obj = new HRManagement();
        return obj;
    }
    
    public HRManagement HRManagementDataOnDemand.getSpecificHRManagement(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        HRManagement obj = data.get(index);
        return HRManagement.findHRManagement(obj.getId());
    }
    
    public HRManagement HRManagementDataOnDemand.getRandomHRManagement() {
        init();
        HRManagement obj = data.get(rnd.nextInt(data.size()));
        return HRManagement.findHRManagement(obj.getId());
    }
    
    public boolean HRManagementDataOnDemand.modifyHRManagement(HRManagement obj) {
        return false;
    }
    
}
