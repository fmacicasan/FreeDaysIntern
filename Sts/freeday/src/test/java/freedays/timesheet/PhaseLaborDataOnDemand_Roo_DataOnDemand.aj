// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package freedays.timesheet;

import freedays.timesheet.PhaseLabor;
import java.lang.Float;
import java.security.SecureRandom;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect PhaseLaborDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PhaseLaborDataOnDemand: @Component;
    
    private Random PhaseLaborDataOnDemand.rnd = new SecureRandom();
    
    public PhaseLabor PhaseLaborDataOnDemand.getNewTransientPhaseLabor(int index) {
        PhaseLabor obj = new PhaseLabor();
        setLaborbilling(obj, index);
        setPattern(obj, index);
        setPercentage(obj, index);
        setPhase(obj, index);
        setProject(obj, index);
        return obj;
    }
    
    public void PhaseLaborDataOnDemand.setPercentage(PhaseLabor obj, int index) {
        Float percentage = new Integer(index).floatValue();
        obj.setPercentage(percentage);
    }
    
    public PhaseLabor PhaseLaborDataOnDemand.getSpecificPhaseLabor(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        PhaseLabor obj = data.get(index);
        return PhaseLabor.findPhaseLabor(obj.getId());
    }
    
    public PhaseLabor PhaseLaborDataOnDemand.getRandomPhaseLabor() {
        init();
        PhaseLabor obj = data.get(rnd.nextInt(data.size()));
        return PhaseLabor.findPhaseLabor(obj.getId());
    }
    
    public boolean PhaseLaborDataOnDemand.modifyPhaseLabor(PhaseLabor obj) {
        return false;
    }
    
}
